# 2025-06-30    Day - 41    Docker - Introduction

# Topics
- Docker Installation
- Basic Commands
    - docker info
    - docker pull {image}
    - docker run {image}
    - docker run -d -it {image} sh
    - docker run -d -p 8080:80 {image}
    - docker ps
    - docker ps -a
    - docker rm {container}
    - docker start {container}
    - docker stop {container}
    - docker logs {container}
    - docker images
    - docker rmi {image}
    - docker system prune

## Links
- https://docs.docker.com/get-started/docker-overview/
- https://www.datacamp.com/tutorial/docker-prune